# Metric catalogue

Versioned definitions live in `packages/contracts/src/metrics/catalogue.ts`.

Rules:
- Fail-safe is `withhold` or `flag_low_confidence` — never invent values in UI/LLM
- Footwork court metrics require valid court calibration
- Technique contact metrics prefer racket + shuttle tracks when available
